ALTER TABLE "book_copies" ADD COLUMN "lost_at" TIMESTAMP(3); CREATE TABLE "saved_reports" ("id" SERIAL NOT NULL, "owner_id" INTEGER NOT NULL, "name" TEXT NOT NULL, "definition" JSONB NOT NULL, "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "saved_reports_pkey" PRIMARY KEY ("id")); CREATE INDEX "saved_reports_owner_id_idx" ON "saved_reports"("owner_id"); CREATE INDEX "saved_reports_updated_at_idx" ON "saved_reports"("updated_at"); ALTER TABLE "saved_reports" ADD CONSTRAINT "saved_reports_owner_id_fkey" FOREIGN KEY ("owner_id") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
